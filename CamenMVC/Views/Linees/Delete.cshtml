@model CamenMVC.Models.Linee

@{
    ViewBag.Title = "Cancella area";
}

<h2>@ViewBag.Title</h2>

<h3>Sei sicuro di voler cancellare l'area?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Linea)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Linea)
        </dd>

    </dl>
    <hr />
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Cancella" class="btn btn-danger" />
            @Html.ActionLink("Annulla", "Index", null, new { @class = "btn btn-primary" })
        </div>
    }
</div>

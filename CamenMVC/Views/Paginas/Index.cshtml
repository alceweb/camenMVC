@model IEnumerable<CamenMVC.Models.Pagina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestoSmenu.TestoSmenu)
        </th>
        <th>
            @Html.DisplayNameFor(model=>model.Pagina_Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestoSmenu.TestoSmenu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pagina_Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pagina_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pagina_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pagina_Id })
        </td>
    </tr>
}

</table>

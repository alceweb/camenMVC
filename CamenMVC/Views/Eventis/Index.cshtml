@model IEnumerable<CamenMVC.Models.Eventi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Evento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Evento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Evento_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Evento_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Evento_Id })
        </td>
    </tr>
}

</table>
